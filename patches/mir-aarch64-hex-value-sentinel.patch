diff --git a/mir-gen-aarch64.c b/mir-gen-aarch64.c
index 7d9fcca..d66a6f6 100644
--- a/mir-gen-aarch64.c
+++ b/mir-gen-aarch64.c
@@ -2168,6 +2168,7 @@ static void out_insn (gen_ctx_t gen_ctx, MIR_insn_t insn, const char *replacemen
-    char ch, ch2, start_ch, d;
+    char ch, ch2, start_ch;
+    int d; /* hex_value() returns -1; plain char is unsigned on Linux AArch64. */
     uint32_t opcode = 0, opcode_mask = 0xffffffff;
     int rd = -1, rn = -1, rm = -1, ra = -1, disp = -1, scale = -1;
     int immr = -1, imms = -1, imm16 = -1, imm16_shift = -1, imm12 = -1, imm12_shift = -1;
     MIR_op_t op;
     int label_ref_num = -1, switch_table_addr_p = FALSE;
