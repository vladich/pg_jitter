name: "PG 14 \xB7 Windows x86_64"
on:
  push:
    branches: [master]
    paths-ignore: ['**.md', 'LICENSE', '.gitignore', 'img/**', 'bench/**']
jobs:
  build:
    uses: ./.github/workflows/build-reusable.yml
    with:
      postgres-version: "14"
      os: windows-2022
