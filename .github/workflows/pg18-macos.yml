name: "PG 18 \xB7 macOS ARM64"
on:
  push:
    branches: [master]
    paths-ignore: ['**.md', 'LICENSE', '.gitignore', 'tests/**', 'img/**', 'bench/**']
jobs:
  build:
    uses: ./.github/workflows/build-reusable.yml
    with:
      postgres-version: "18"
      os: macos-15
