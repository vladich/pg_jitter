name: "PG 16 \xB7 Linux x86_64"
on:
  push:
    branches: [master]
    paths-ignore: ['**.md', 'LICENSE', '.gitignore', 'img/**', 'bench/**']
jobs:
  build:
    uses: ./.github/workflows/build-reusable.yml
    with:
      postgres-version: "16"
      os: ubuntu-24.04
