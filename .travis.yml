language: c
os: linux
dist: jammy

# Only build when triggered via API (from GitHub Actions) — not on push/PR directly
if: type = api

jobs:
  include:
    - arch: ppc64le
      env: PG_VERSION=17
    - arch: s390x
      env: PG_VERSION=14

before_install:
  # PGDG apt repository
  - sudo install -d /usr/share/postgresql-common/pgdg
  - sudo curl -o /usr/share/postgresql-common/pgdg/apt.postgresql.org.asc --fail https://www.postgresql.org/media/keys/ACCC4CF8.asc
  - echo "deb [signed-by=/usr/share/postgresql-common/pgdg/apt.postgresql.org.asc] https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" | sudo tee /etc/apt/sources.list.d/pgdg.list
  - sudo apt-get update -qq
  # s390x: PGDG may not carry all PG versions — fall back to Ubuntu's default PG 14
  - sudo apt-get install -y -qq cmake g++ libicu-dev postgresql-server-dev-${PG_VERSION} || sudo apt-get install -y -qq cmake g++ libicu-dev postgresql-server-dev-14

install:
  # Clone dependencies
  - git clone --depth 1 https://github.com/zherczeg/sljit.git deps/sljit
  - git clone --depth 1 https://github.com/vladich/mir-patched.git deps/mir
  - git clone --depth 1 https://github.com/ashvardanian/StringZilla.git deps/stringzilla
  # PCRE2 with JIT (SLJIT supports both ppc64le and s390x)
  - git clone --depth 1 https://github.com/PCRE2Project/pcre2.git deps/pcre2
  - mkdir -p deps/pcre2/build
  - >-
    cmake -S deps/pcre2 -B deps/pcre2/build
    -DCMAKE_BUILD_TYPE=Release
    -DPCRE2_SUPPORT_JIT=ON
    -DPCRE2_SUPPORT_UNICODE=ON
    -DPCRE2_BUILD_PCRE2_8=ON
    -DBUILD_SHARED_LIBS=OFF
    -DPCRE2_BUILD_PCRE2GREP=OFF
    -DPCRE2_BUILD_TESTS=OFF
    -DPCRE2_STATIC_PIC=ON
  # Limit parallelism — Travis CI ppc64le/s390x runners have limited RAM
  - cmake --build deps/pcre2/build --target pcre2-8-static -j 2

script:
  - PG_CONFIG=$(find /usr/lib/postgresql -name pg_config 2>/dev/null | sort -V | tail -1)
  - echo "Using $PG_CONFIG"
  - $PG_CONFIG --version
  - >-
    cmake -B build
    -DPG_CONFIG=${PG_CONFIG}
    -DPG_JITTER_BACKENDS="sljit;mir"
    -DSLJIT_DIR=${TRAVIS_BUILD_DIR}/deps/sljit
    -DMIR_DIR=${TRAVIS_BUILD_DIR}/deps/mir
    -DSTRINGZILLA_DIR=${TRAVIS_BUILD_DIR}/deps/stringzilla
    -DPCRE2_DIR=${TRAVIS_BUILD_DIR}/deps/pcre2
    -DCMAKE_BUILD_TYPE=Release
  - cmake --build build --parallel 2
  - ls -la build/pg_jitter*.so
